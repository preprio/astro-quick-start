---
import Layout from '../layouts/Layout.astro';
import {Prepr} from '../lib/prepr.js';
import GetUpdates from '../queries/get-updates.js';

const response = await Prepr(GetUpdates, {})

const { json } = await response.json()

const updates = json.Drievoor12updates.items
---

<Layout title="Test test">
	<h1>
		3 voor 12 updates
	</h1>
	<ul>
		{
			updates.items.map((update) => (
				<li>
					<a href="{update._slug}">{update.title}</a>
				</li>
			))
		}
	</ul>
</Layout>